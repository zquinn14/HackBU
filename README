# Libraries Used

- `python-twitter`
- `nltk`

To install any of these libraries, run `pip install <name>`

# NLTK Downloads

```python
nltk.download('punkt')
nltk.download('averaged_perceptron_tagger')
```